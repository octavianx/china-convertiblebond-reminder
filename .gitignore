# 本地配置 —— 含 Bark 推送凭证,绝不入库
config.json

# 构建产物(Makefile 会把 config.json 拷进来,可能夹带凭证)
/release/

# macOS
.DS_Store

# 规划文档放在仓库外(顶层私有区),不入库
docs/

# 本地测试(含真实设备 key,不公开;逻辑同 parser_test.go 那套)
config_test.go
